<h1>Lista de comentarios</h1>

<table>
  <tr>
    <th>Comentario</th>
    <th>Body</th>
    <th>Post_id</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

	<% if @comments %>
		<% @comments.each do |c| %>
		  <tr>
			<td><%= c.commenter %></td>
			<td><%= c.body %></td>
			<td><%= c.post_id %></td>
		  </tr>
		<% end %>
	<% else%>
		  <tr>
			<td>No hay comentarios</td>
		  </tr>
	<% end%>
</table>

<br />

<%= link_to 'Nuevo Comentario', :controller => 'comment', :action => 'new', c.post_id %>

